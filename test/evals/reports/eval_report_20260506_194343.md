# Phase 2 评测报告

**评测时间**: 2026-05-06 19:43:43
**评测题目数**: 15

## 总体统计

| 指标 | 数量 | 比例 |
|------|------|------|
| 表召回命中 | 5/15 | 33.3% |
| 字段召回命中 | 1/15 | 6.7% |
| SQL可执行 | 6/15 | 40.0% |
| 执行错误 | 9/15 | 60.0% |

## 分类别统计

| 类别 | 题目数 | 表召回命中 | SQL可执行 |
|------|--------|------------|-----------|
| 经营指标 | 5 | 2/5 | 4/5 |
| 商品维度 | 5 | 0/5 | 2/5 |
| 趋势对比 | 5 | 3/5 | 0/5 |

## 详细结果

| ID | 类别 | 问题 | 表召回 | SQL可执行 | 错误 |
|----|------|------|--------|-----------|------|
| 1 | 经营指标 | 本月订单量是多少 | ✅ | ✅ |  |
| 2 | 经营指标 | 最近30天收入金额是多少 | ❌ | ✅ |  |
| 3 | 经营指标 | 退款率最高的前十个商品是什么 | ❌ | ❌ | (asyncmy.errors.ProgrammingError) (1103, "Incorrec... |
| 4 | 经营指标 | 本月优惠券核销金额是多少 | ❌ | ✅ |  |
| 5 | 经营指标 | 最近7天日均订单量是多少 | ✅ | ✅ |  |
| 6 | 商品维度 | 哪个目的地订单量最多 | ❌ | ❌ | (asyncmy.errors.ProgrammingError) (1103, "Incorrec... |
| 7 | 商品维度 | 哪种商品类型销售额最高 | ❌ | ❌ | (asyncmy.errors.OperationalError) (1059, "Identifi... |
| 8 | 商品维度 | 哪个供应商退款率最高 | ❌ | ❌ | (asyncmy.errors.ProgrammingError) (1103, "Incorrec... |
| 9 | 商品维度 | 各渠道订单占比是多少 | ❌ | ✅ |  |
| 10 | 商品维度 | 五星酒店平均房价是多少 | ❌ | ✅ |  |
| 11 | 趋势对比 | 最近三个月订单量变化趋势 | ✅ | ❌ | (asyncmy.errors.ProgrammingError) (1103, "Incorrec... |
| 12 | 趋势对比 | 本月和上月机票销量对比 | ❌ | ❌ | (asyncmy.errors.ProgrammingError) (1103, "Incorrec... |
| 13 | 趋势对比 | 节假日和非节假日客单价差异 | ✅ | ❌ | (asyncmy.errors.ProgrammingError) (1103, "Incorrec... |
| 14 | 趋势对比 | 各商品类型周销量排名变化 | ❌ | ❌ | (asyncmy.errors.ProgrammingError) (1103, "Incorrec... |
| 15 | 趋势对比 | 最近30天每日收入趋势 | ✅ | ❌ | (asyncmy.errors.ProgrammingError) (1103, "Incorrec... |

## 错误归因

### 评测 3: 退款率最高的前十个商品是什么
- **错误**: (asyncmy.errors.ProgrammingError) (1103, "Incorrect table name '`sql\nSELECT \n    oi.product_id,\n    MAX(oi.product_name) AS product_name,\n    COUNT(*) AS total_item'")
[SQL: EXPLAIN ```sql
SELECT 
    oi.product_id,
    MAX(oi.product_name) AS product_name,
    COUNT(*) AS total_items,
    COUNT(DISTINCT rr.order_item_id) AS refunded_items,
    COUNT(DISTINCT rr.order_item_id) * 1.0 / COUNT(*) AS refund_rate
FROM order_items oi
LEFT JOIN refund_requests rr ON oi.id = rr.order_item_id
GROUP BY oi.product_id
ORDER BY refund_rate DESC
LIMIT 10;
```]
(Background on this error at: https://sqlalche.me/e/20/f405)
- **归因**: 召回缺失（期望的表未被召回）

### 评测 6: 哪个目的地订单量最多
- **错误**: (asyncmy.errors.ProgrammingError) (1103, "Incorrect table name '`sql\nSELECT \n    a.area_full_name AS destination,\n    COUNT(*) AS order_count\nFROM (\n    -- 酒店??'")
[SQL: EXPLAIN ```sql
SELECT 
    a.area_full_name AS destination,
    COUNT(*) AS order_count
FROM (
    -- 酒店：订单项关联到客房类型，再关联到酒店，取所在区域
    SELECT h.area_id
    FROM order_items oi
    JOIN hotel_room_types hrt ON oi.product_id = hrt.id AND oi.product_type_code = 'hotel'
    JOIN hotels h ON hrt.hotel_id = h.id
    UNION ALL
    -- 景点门票：订单项关联到门票类型，再关联到景点，取所在区域
    SELECT ss.area_id
    FROM order_items oi
    JOIN scenic_ticket_types stt ON oi.product_id = stt.id AND oi.product_type_code = 'scenic'
    JOIN scenic_spots ss ON stt.scenic_spot_id = ss.id
    UNION ALL
    -- 航班：订单项关联到航班出发，再关联到航班路线，取到达区域
    SELECT fr.arrival_area_id
    FROM order_items oi
    JOIN flight_departures fd ON oi.product_id = fd.id AND oi.product_type_code = 'flight'
    JOIN flight_routes fr ON fd.flight_route_id = fr.id
    UNION ALL
    -- 火车：订单项关联到火车出发，再关联到火车路线，取到达区域
    SELECT tr.arrival_area_id
    FROM order_items oi
    JOIN train_departures td ON oi.product_id = td.id AND oi.product_type_code = 'train'
    JOIN train_routes tr ON td.train_route_id = tr.id
    UNION ALL
    -- 大巴：订单项关联到大巴出发，再关联到大巴路线，取到达区域
    SELECT br.arrival_area_id
    FROM order_items oi
    JOIN bus_departures bd ON oi.product_id = bd.id AND oi.product_type_code = 'bus'
    JOIN bus_routes br ON bd.bus_route_id = br.id
    UNION ALL
    -- 接送服务：订单项直接关联到接送服务，取服务所在区域
    SELECT ts.area_id
    FROM order_items oi
    JOIN transfer_services ts ON oi.product_id = ts.id AND oi.product_type_code = 'transfer'
) AS t
JOIN areas a ON t.area_id = a.id
GROUP BY a.id, a.area_full_name
ORDER BY order_count DESC
LIMIT 1;
```]
(Background on this error at: https://sqlalche.me/e/20/f405)
- **归因**: 召回缺失（期望的表未被召回）

### 评测 7: 哪种商品类型销售额最高
- **错误**: (asyncmy.errors.OperationalError) (1059, "Identifier name '`sql\nSELECT \n    product_type_code,\n    SUM(sale_amount) AS total_sales\nFROM order_items\nGROUP BY pr' is too long")
[SQL: EXPLAIN ```sql
SELECT 
    product_type_code,
    SUM(sale_amount) AS total_sales
FROM order_items
GROUP BY product_type_code
ORDER BY total_sales DESC
LIMIT 1;
```]
(Background on this error at: https://sqlalche.me/e/20/e3q8)
- **归因**: 召回缺失（期望的表未被召回）

### 评测 8: 哪个供应商退款率最高
- **错误**: (asyncmy.errors.ProgrammingError) (1103, "Incorrect table name '`sql\nSELECT \n    s.supplier_name,\n    COUNT(DISTINCT sub.order_id) AS total_orders,\n    COUNT(DISTIN'")
[SQL: EXPLAIN ```sql
SELECT 
    s.supplier_name,
    COUNT(DISTINCT sub.order_id) AS total_orders,
    COUNT(DISTINCT CASE WHEN rr.id IS NOT NULL THEN sub.order_id END) AS refunded_orders,
    COUNT(DISTINCT CASE WHEN rr.id IS NOT NULL THEN sub.order_id END) * 1.0 
        / COUNT(DISTINCT sub.order_id) AS refund_rate
FROM (
    SELECT 
        oi.id AS order_item_id,
        oi.order_id,
        CASE 
            WHEN oi.product_type_code = 'hotel' THEN h.supplier_id
            WHEN oi.product_type_code = 'scenic' THEN sc.supplier_id
            WHEN oi.product_type_code = 'flight' THEN fr.supplier_id
            WHEN oi.product_type_code = 'bus' THEN br.supplier_id
            WHEN oi.product_type_code = 'train' THEN tr.supplier_id
            WHEN oi.product_type_code = 'transfer' THEN ts.supplier_id
        END AS supplier_id
    FROM order_items oi
    LEFT JOIN hotels h ON oi.product_type_code = 'hotel' AND oi.product_id = h.id
    LEFT JOIN scenic_spots sc ON oi.product_type_code = 'scenic' AND oi.product_id = sc.id
    LEFT JOIN flight_routes fr ON oi.product_type_code = 'flight' AND oi.product_id = fr.id
    LEFT JOIN bus_routes br ON oi.product_type_code = 'bus' AND oi.product_id = br.id
    LEFT JOIN train_routes tr ON oi.product_type_code = 'train' AND oi.product_id = tr.id
    LEFT JOIN transfer_services ts ON oi.product_type_code = 'transfer' AND oi.product_id = ts.id
) sub
LEFT JOIN refund_requests rr ON rr.order_item_id = sub.order_item_id
JOIN suppliers s ON s.id = sub.supplier_id
GROUP BY s.id, s.supplier_name
ORDER BY refund_rate DESC
LIMIT 1;
```]
(Background on this error at: https://sqlalche.me/e/20/f405)
- **归因**: 召回缺失（期望的表未被召回）

### 评测 11: 最近三个月订单量变化趋势
- **错误**: (asyncmy.errors.ProgrammingError) (1103, "Incorrect table name '`sql\nSELECT \n    DATE_FORMAT(o.created_at, '%x-W%v') AS week_label,\n    COUNT(o.id) AS order_count\nF'")
[SQL: EXPLAIN ```sql
SELECT 
    DATE_FORMAT(o.created_at, '%%x-W%%v') AS week_label,
    COUNT(o.id) AS order_count
FROM orders o
WHERE o.created_at >= DATE_SUB('2026-05-06', INTERVAL 3 MONTH)
  AND o.created_at < DATE_ADD('2026-05-06', INTERVAL 1 DAY)
GROUP BY week_label
ORDER BY week_label;
```]
(Background on this error at: https://sqlalche.me/e/20/f405)
- **归因**: SQL生成错误

### 评测 12: 本月和上月机票销量对比
- **错误**: (asyncmy.errors.ProgrammingError) (1103, "Incorrect table name '`sql\nSELECT \n    '本月' AS period,\n    COALESCE(SUM(fci.sold_inventory), 0) AS sales_volume\nFROM f'")
[SQL: EXPLAIN ```sql
SELECT 
    '本月' AS period,
    COALESCE(SUM(fci.sold_inventory), 0) AS sales_volume
FROM flight_cabin_inventory fci
JOIN flight_departures fd ON fci.flight_departure_id = fd.id
WHERE fd.departure_time >= DATE_FORMAT('2026-05-06', '%%Y-%%m-01')
  AND fd.departure_time < DATE_FORMAT(DATE_ADD('2026-05-06', INTERVAL 1 MONTH), '%%Y-%%m-01')
UNION ALL
SELECT 
    '上月' AS period,
    COALESCE(SUM(fci.sold_inventory), 0) AS sales_volume
FROM flight_cabin_inventory fci
JOIN flight_departures fd ON fci.flight_departure_id = fd.id
WHERE fd.departure_time >= DATE_FORMAT(DATE_SUB('2026-05-06', INTERVAL 1 MONTH), '%%Y-%%m-01')
  AND fd.departure_time < DATE_FORMAT('2026-05-06', '%%Y-%%m-01');
```]
(Background on this error at: https://sqlalche.me/e/20/f405)
- **归因**: 召回缺失（期望的表未被召回）

### 评测 13: 节假日和非节假日客单价差异
- **错误**: (asyncmy.errors.ProgrammingError) (1103, "Incorrect table name '`sql\nWITH holiday_orders AS (\n    SELECT AVG(paid_amount) AS aov\n    FROM orders\n    WHERE DAYOFWEEK'")
[SQL: EXPLAIN ```sql
WITH holiday_orders AS (
    SELECT AVG(paid_amount) AS aov
    FROM orders
    WHERE DAYOFWEEK(created_at) IN (1, 7)  -- 周日(1)和周六(7)
      AND paid_amount IS NOT NULL
),
non_holiday_orders AS (
    SELECT AVG(paid_amount) AS aov
    FROM orders
    WHERE DAYOFWEEK(created_at) NOT IN (1, 7)
      AND paid_amount IS NOT NULL
)
SELECT 
    holiday_orders.aov AS holiday_aov,
    non_holiday_orders.aov AS non_holiday_aov,
    holiday_orders.aov - non_holiday_orders.aov AS difference
FROM holiday_orders, non_holiday_orders;
```]
(Background on this error at: https://sqlalche.me/e/20/f405)
- **归因**: SQL生成错误

### 评测 14: 各商品类型周销量排名变化
- **错误**: (asyncmy.errors.ProgrammingError) (1103, "Incorrect table name '`sql\nWITH weekly_sales AS (\n    SELECT\n        YEARWEEK(oi.created_at, 1) AS week_number,\n        oi'")
[SQL: EXPLAIN ```sql
WITH weekly_sales AS (
    SELECT
        YEARWEEK(oi.created_at, 1) AS week_number,
        oi.product_type_code,
        COUNT(*) AS sales_volume
    FROM order_items oi
    WHERE oi.created_at IS NOT NULL
    GROUP BY week_number, oi.product_type_code
)
SELECT
    week_number,
    product_type_code,
    sales_volume,
    RANK() OVER (PARTITION BY week_number ORDER BY sales_volume DESC) AS rank_in_week
FROM weekly_sales
ORDER BY week_number, rank_in_week;
```]
(Background on this error at: https://sqlalche.me/e/20/f405)
- **归因**: 召回缺失（期望的表未被召回）

### 评测 15: 最近30天每日收入趋势
- **错误**: (asyncmy.errors.ProgrammingError) (1103, "Incorrect table name '`sql\nSELECT\n    DATE(created_at) AS day,\n    SUM(paid_amount) AS revenue\nFROM orders\nWHERE created_a'")
[SQL: EXPLAIN ```sql
SELECT
    DATE(created_at) AS day,
    SUM(paid_amount) AS revenue
FROM orders
WHERE created_at >= DATE_SUB(CURDATE(), INTERVAL 30 DAY)
  AND created_at < DATE_ADD(CURDATE(), INTERVAL 1 DAY)
GROUP BY day
ORDER BY day;
```]
(Background on this error at: https://sqlalche.me/e/20/f405)
- **归因**: SQL生成错误

