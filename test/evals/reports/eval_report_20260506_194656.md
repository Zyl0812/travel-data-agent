# Phase 2 评测报告

**评测时间**: 2026-05-06 19:46:56
**评测题目数**: 15

## 总体统计

| 指标 | 数量 | 比例 |
|------|------|------|
| 表召回命中 | 4/15 | 26.7% |
| 字段召回命中 | 0/15 | 0.0% |
| SQL可执行 | 5/15 | 33.3% |
| 执行错误 | 10/15 | 66.7% |

## 分类别统计

| 类别 | 题目数 | 表召回命中 | SQL可执行 |
|------|--------|------------|-----------|
| 经营指标 | 5 | 2/5 | 3/5 |
| 商品维度 | 5 | 0/5 | 2/5 |
| 趋势对比 | 5 | 2/5 | 0/5 |

## 详细结果

| ID | 类别 | 问题 | 表召回 | SQL可执行 | 错误 |
|----|------|------|--------|-----------|------|
| 1 | 经营指标 | 本月订单量是多少 | ✅ | ✅ |  |
| 2 | 经营指标 | 最近30天收入金额是多少 | ❌ | ✅ |  |
| 3 | 经营指标 | 退款率最高的前十个商品是什么 | ❌ | ❌ | (asyncmy.errors.ProgrammingError) (1103, "Incorrec... |
| 4 | 经营指标 | 本月优惠券核销金额是多少 | ❌ | ❌ | Connection error. |
| 5 | 经营指标 | 最近7天日均订单量是多少 | ✅ | ✅ |  |
| 6 | 商品维度 | 哪个目的地订单量最多 | ❌ | ❌ | (asyncmy.errors.ProgrammingError) (1103, "Incorrec... |
| 7 | 商品维度 | 哪种商品类型销售额最高 | ❌ | ❌ | (asyncmy.errors.OperationalError) (1059, "Identifi... |
| 8 | 商品维度 | 哪个供应商退款率最高 | ❌ | ❌ | (asyncmy.errors.ProgrammingError) (1103, "Incorrec... |
| 9 | 商品维度 | 各渠道订单占比是多少 | ❌ | ✅ |  |
| 10 | 商品维度 | 五星酒店平均房价是多少 | ❌ | ✅ |  |
| 11 | 趋势对比 | 最近三个月订单量变化趋势 | ✅ | ❌ | (asyncmy.errors.ProgrammingError) (1103, "Incorrec... |
| 12 | 趋势对比 | 本月和上月机票销量对比 | ❌ | ❌ | (asyncmy.errors.ProgrammingError) (1103, "Incorrec... |
| 13 | 趋势对比 | 节假日和非节假日客单价差异 | ❌ | ❌ | (asyncmy.errors.ProgrammingError) (1103, "Incorrec... |
| 14 | 趋势对比 | 各商品类型周销量排名变化 | ❌ | ❌ | (asyncmy.errors.OperationalError) (1690, "BIGINT U... |
| 15 | 趋势对比 | 最近30天每日收入趋势 | ✅ | ❌ | (asyncmy.errors.ProgrammingError) (1103, "Incorrec... |

## 错误归因

### 评测 3: 退款率最高的前十个商品是什么
- **错误**: (asyncmy.errors.ProgrammingError) (1103, "Incorrect table name '`sql\nSELECT \n    oi.product_name,\n    COUNT(DISTINCT oi.id) AS total_items,\n    COUNT(DISTINCT rr.or'")
[SQL: EXPLAIN ```sql
SELECT 
    oi.product_name,
    COUNT(DISTINCT oi.id) AS total_items,
    COUNT(DISTINCT rr.order_item_id) AS refunded_items,
    COUNT(DISTINCT rr.order_item_id) * 100.0 / COUNT(DISTINCT oi.id) AS refund_rate
FROM order_items oi
LEFT JOIN refund_requests rr ON oi.id = rr.order_item_id
GROUP BY oi.product_name
ORDER BY refund_rate DESC
LIMIT 10
```]
(Background on this error at: https://sqlalche.me/e/20/f405)
- **归因**: 召回缺失（期望的表未被召回）

### 评测 4: 本月优惠券核销金额是多少
- **错误**: Connection error.
- **归因**: 召回缺失（期望的表未被召回）

### 评测 6: 哪个目的地订单量最多
- **错误**: (asyncmy.errors.ProgrammingError) (1103, "Incorrect table name '`sql\nSELECT \n    a.area_full_name AS destination,\n    COUNT(DISTINCT oi.order_id) AS order_count\nFRO'")
[SQL: EXPLAIN ```sql
SELECT 
    a.area_full_name AS destination,
    COUNT(DISTINCT oi.order_id) AS order_count
FROM order_items oi
LEFT JOIN hotels h ON oi.product_type_code = 'HOTEL' AND oi.product_id = h.id
LEFT JOIN scenic_spots s ON oi.product_type_code = 'SCENIC' AND oi.product_id = s.id
LEFT JOIN flight_routes fr ON oi.product_type_code = 'FLIGHT' AND oi.product_id = fr.id
LEFT JOIN bus_routes br ON oi.product_type_code = 'BUS' AND oi.product_id = br.id
LEFT JOIN train_routes tr ON oi.product_type_code = 'TRAIN' AND oi.product_id = tr.id
LEFT JOIN transfer_services ts ON oi.product_type_code = 'TRANSFER' AND oi.product_id = ts.id
LEFT JOIN areas a 
    ON a.id = COALESCE(h.area_id, s.area_id, fr.arrival_area_id, br.arrival_area_id, tr.arrival_area_id, ts.area_id)
WHERE a.id IS NOT NULL
GROUP BY a.area_full_name
ORDER BY order_count DESC
LIMIT 1;
```]
(Background on this error at: https://sqlalche.me/e/20/f405)
- **归因**: 召回缺失（期望的表未被召回）

### 评测 7: 哪种商品类型销售额最高
- **错误**: (asyncmy.errors.OperationalError) (1059, "Identifier name '`sql\nSELECT product_type_code, SUM(sale_amount) AS total_sales\nFROM order_items\nGROUP BY product_typ' is too long")
[SQL: EXPLAIN ```sql
SELECT product_type_code, SUM(sale_amount) AS total_sales
FROM order_items
GROUP BY product_type_code
ORDER BY total_sales DESC
LIMIT 1
```]
(Background on this error at: https://sqlalche.me/e/20/e3q8)
- **归因**: 召回缺失（期望的表未被召回）

### 评测 8: 哪个供应商退款率最高
- **错误**: (asyncmy.errors.ProgrammingError) (1103, "Incorrect table name '`sql\nSELECT \n    s.supplier_name,\n    COUNT(DISTINCT oi.id) AS total_items,\n    COUNT(DISTINCT rr.or'")
[SQL: EXPLAIN ```sql
SELECT 
    s.supplier_name,
    COUNT(DISTINCT oi.id) AS total_items,
    COUNT(DISTINCT rr.order_item_id) AS refunded_items,
    IFNULL(COUNT(DISTINCT rr.order_item_id) * 100.0 / NULLIF(COUNT(DISTINCT oi.id), 0), 0) AS refund_rate
FROM order_items oi
LEFT JOIN refund_requests rr ON oi.id = rr.order_item_id
LEFT JOIN (
    SELECT id, supplier_id, 'bus_routes' AS product_type FROM bus_routes
    UNION ALL
    SELECT id, supplier_id, 'flight_routes' FROM flight_routes
    UNION ALL
    SELECT id, supplier_id, 'hotels' FROM hotels
    UNION ALL
    SELECT id, supplier_id, 'scenic_spots' FROM scenic_spots
    UNION ALL
    SELECT id, supplier_id, 'transfer_services' FROM transfer_services
    UNION ALL
    SELECT id, supplier_id, 'train_routes' FROM train_routes
) AS products 
    ON oi.product_id = products.id 
    AND oi.product_type_code = products.product_type
JOIN suppliers s ON products.supplier_id = s.id
GROUP BY s.id, s.supplier_name
ORDER BY refund_rate DESC
LIMIT 1;
```]
(Background on this error at: https://sqlalche.me/e/20/f405)
- **归因**: 召回缺失（期望的表未被召回）

### 评测 11: 最近三个月订单量变化趋势
- **错误**: (asyncmy.errors.ProgrammingError) (1103, "Incorrect table name '`sql\nSELECT \n    YEARWEEK(created_at, 1) AS week_key,\n    COUNT(id) AS order_count\nFROM orders\nWHERE'")
[SQL: EXPLAIN ```sql
SELECT 
    YEARWEEK(created_at, 1) AS week_key,
    COUNT(id) AS order_count
FROM orders
WHERE created_at >= DATE_SUB('2026-05-06', INTERVAL 3 MONTH)
  AND created_at < DATE_ADD('2026-05-06', INTERVAL 1 DAY)
GROUP BY week_key
ORDER BY week_key;
```]
(Background on this error at: https://sqlalche.me/e/20/f405)
- **归因**: SQL生成错误

### 评测 12: 本月和上月机票销量对比
- **错误**: (asyncmy.errors.ProgrammingError) (1103, "Incorrect table name '`sql\nSELECT \n    '本月' AS period,\n    COUNT(DISTINCT oi.id) AS sale_count\nFROM orders o\nJOIN orde'")
[SQL: EXPLAIN ```sql
SELECT 
    '本月' AS period,
    COUNT(DISTINCT oi.id) AS sale_count
FROM orders o
JOIN order_items oi ON o.id = oi.order_id
WHERE oi.product_type_code = 'FLIGHT'
  AND o.created_at >= DATE_FORMAT(CURDATE(), '%%Y-%%m-01')

UNION ALL

SELECT 
    '上月' AS period,
    COUNT(DISTINCT oi.id) AS sale_count
FROM orders o
JOIN order_items oi ON o.id = oi.order_id
WHERE oi.product_type_code = 'FLIGHT'
  AND o.created_at >= DATE_FORMAT(DATE_SUB(CURDATE(), INTERVAL 1 MONTH), '%%Y-%%m-01')
  AND o.created_at < DATE_FORMAT(CURDATE(), '%%Y-%%m-01');
```]
(Background on this error at: https://sqlalche.me/e/20/f405)
- **归因**: 召回缺失（期望的表未被召回）

### 评测 13: 节假日和非节假日客单价差异
- **错误**: (asyncmy.errors.ProgrammingError) (1103, "Incorrect table name '`sql\nSELECT \n    '节假日' AS period,\n    COALESCE(SUM(paid_amount) / COUNT(*), 0) AS avg_order_va'")
[SQL: EXPLAIN ```sql
SELECT 
    '节假日' AS period,
    COALESCE(SUM(paid_amount) / COUNT(*), 0) AS avg_order_value
FROM orders
WHERE WEEKDAY(created_at) IN (5, 6)
UNION ALL
SELECT 
    '非节假日' AS period,
    COALESCE(SUM(paid_amount) / COUNT(*), 0) AS avg_order_value
FROM orders
WHERE WEEKDAY(created_at) NOT IN (5, 6);
```]
(Background on this error at: https://sqlalche.me/e/20/f405)
- **归因**: 召回缺失（期望的表未被召回）

### 评测 14: 各商品类型周销量排名变化
- **错误**: (asyncmy.errors.OperationalError) (1690, "BIGINT UNSIGNED value is out of range in '(`lag(rs.current_rank) OVER (PARTITION BY rs.product_type_code ORDER BY rs.week_period ) ` - `ranked_sales`.`current_rank`)'")
[SQL: WITH weekly_sales AS (
    SELECT 
        DATE_FORMAT(oi.created_at, '%%x-W%%v') AS week_period,
        oi.product_type_code,
        COUNT(*) AS sales_volume
    FROM order_items oi
    GROUP BY week_period, oi.product_type_code
),
ranked_sales AS (
    SELECT 
        week_period,
        product_type_code,
        sales_volume,
        DENSE_RANK() OVER (PARTITION BY week_period ORDER BY sales_volume DESC) AS current_rank
    FROM weekly_sales
)
SELECT 
    rs.week_period AS 周,
    rs.product_type_code AS 商品类型,
    rs.sales_volume AS 销量,
    rs.current_rank AS 当前排名,
    LAG(rs.current_rank) OVER (PARTITION BY rs.product_type_code ORDER BY rs.week_period) AS 上一周排名,
    COALESCE(
        LAG(rs.current_rank) OVER (PARTITION BY rs.product_type_code ORDER BY rs.week_period) - rs.current_rank,
        0
    ) AS 排名变化
FROM ranked_sales rs
ORDER BY rs.week_period, rs.current_rank;]
(Background on this error at: https://sqlalche.me/e/20/e3q8)
- **归因**: 召回缺失（期望的表未被召回）

### 评测 15: 最近30天每日收入趋势
- **错误**: (asyncmy.errors.ProgrammingError) (1103, "Incorrect table name '`sql\nSELECT\n    DATE(orders.paid_at) AS day,\n    SUM(orders.paid_amount) AS revenue\nFROM\n    orders\n'")
[SQL: EXPLAIN ```sql
SELECT
    DATE(orders.paid_at) AS day,
    SUM(orders.paid_amount) AS revenue
FROM
    orders
WHERE
    orders.paid_at >= CURDATE() - INTERVAL 30 DAY
    AND orders.paid_at < CURDATE() + INTERVAL 1 DAY
GROUP BY
    DATE(orders.paid_at)
ORDER BY
    day ASC;
```]
(Background on this error at: https://sqlalche.me/e/20/f405)
- **归因**: SQL生成错误

